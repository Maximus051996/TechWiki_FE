# Base URL of the TechWiki backend API
NEXT_PUBLIC_API_URL=http://localhost:4000

# Customer Portal runs on :3000, Admin Portal is served under /admin on the same
# app but with a distinct URL prefix (different from the customer routes).
